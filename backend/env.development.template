# =============================================================================
# EXTERNAL PC - DEVELOPMENT CONFIGURATION TEMPLATE
# =============================================================================
# This is a template for development environment on your external PC.
# Copy this file to .env and configure for your development setup.
#
# USAGE:
#   copy env.development.template .env
#   Edit .env with your development settings
#
# IMPORTANT: Never commit the actual .env file to Git!
# =============================================================================

# =============================================================================
# DATABASE
# =============================================================================
# For development, use SQLite (no server needed)
DATABASE_URL=sqlite:///./askdocs.db

# For PostgreSQL (if you want to match production setup):
# DATABASE_URL=postgresql://username:password@localhost:5432/askdocs_dev

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# Secret key for JWT token signing
# For development, a simple key is fine
JWT_SECRET=development-secret-key-change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=8

# =============================================================================
# MOCK SERVICES (Development Mode)
# =============================================================================
# Set to true to use mock implementations instead of real services
# Recommended for development: keeps it simple and doesn't need real credentials
USE_MOCK_ASKATT=true
USE_MOCK_ASKDOCS=true
USE_MOCK_AZURE_AD=true

# =============================================================================
# AZURE AD OAUTH2 (Mock Mode)
# =============================================================================
# When USE_MOCK_AZURE_AD=true, these values are used for mock authentication
# You can use any values here - they won't connect to real Azure AD
AZURE_TENANT_ID=mock-tenant-id
AZURE_CLIENT_ID=mock-client-id
AZURE_CLIENT_SECRET=mock-client-secret
AZURE_SCOPE=https://graph.microsoft.com/.default

# =============================================================================
# ASKATT API (Optional - Only needed when USE_MOCK_ASKATT=false)
# =============================================================================
# URLs for AskAT&T service (internal company service)
ASKATT_API_BASE_URL_STAGE=https://stage-api.askatt.com
ASKATT_API_BASE_URL_PRODUCTION=https://api.askatt.com

# =============================================================================
# ASKDOCS API (Optional - Only needed when USE_MOCK_ASKDOCS=false)
# =============================================================================
# URLs for AskDocs service
ASKDOCS_API_BASE_URL_STAGE=https://stage-api.askdocs.com
ASKDOCS_API_BASE_URL_PRODUCTION=https://api.askdocs.com

# Configuration API endpoints
ASKDOCS_CONFIG_API_STAGE=https://stage-api.askdocs.com
ASKDOCS_CONFIG_API_PRODUCTION=https://api.askdocs.com

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================
# Comma-separated list of allowed origins for the frontend
# Include all ports where you might run the frontend during development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5174

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Enable debug mode for development
DEBUG=true

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=DEBUG

# =============================================================================
# NOTES
# =============================================================================
# 1. This configuration uses mock services - perfect for development
# 2. No real credentials needed
# 3. SQLite database for simplicity (file-based, no server)
# 4. Debug mode enabled for helpful error messages
# 5. All ports open for CORS to support various development setups
#
# To start development:
#   1. Copy this file: copy env.development.template .env
#   2. (Optional) Adjust values if needed
#   3. Start backend: uvicorn app.main:app --reload
#   4. Start frontend: npm run dev
#   5. Access at: http://localhost:5173
# =============================================================================

